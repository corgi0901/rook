program = expr ("," expr | "\n" expr)*
expr = ident "=" compare | compare
compare = add | add ("==" | "!=" | "<" | "<=" | ">" | ">=")  add
add = mul ("+" mul | "-" mul)*
mul = unary ("*" unary | "/" unary)*
unary = ("+" | "-")? primary
primary = ident | num | "(" compare ")"